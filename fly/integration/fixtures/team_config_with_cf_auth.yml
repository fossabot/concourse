roles:
  - name: member
    cf:
      users: ["some-member"]
  - name: owner
    cf:
      users: ["some-admin"]
      spaceguids: ["some-guid"]
      insecure_orgs: ["some-org"]
  - name: viewer
    cf:
      space_guids: ["some-guid"]
      insecure_spaces: ["some-org:some-space"]
